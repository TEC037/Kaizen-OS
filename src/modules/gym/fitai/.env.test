# Forces local (no-Supabase) mode for the test suite. Overrides .env when
# vitest loads env with mode "test". Committed on purpose for deterministic CI.
VITE_SUPABASE_URL=
VITE_SUPABASE_ANON_KEY=
VITE_SERVERLESS_URL=